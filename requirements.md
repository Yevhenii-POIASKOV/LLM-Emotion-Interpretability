# 🛠 Налаштування робочого середовища (Anaconda + Spyder)

Ця інструкція допоможе розгорнути ідентичне середовище для дослідження нейромереж, щоб уникнути конфліктів версій та помилок інтерпретатора.

## 1. Робота з Anaconda Prompt

**Anaconda Prompt** — це спеціальний термінал, який налаштовує шляхи до інструментів Anaconda. Не використовуйте звичайний `cmd` або PowerShell, якщо не впевнені, що вони налаштовані правильно.

**Як знайти:** Натисніть клавішу `Win`, введіть у пошуку "Anaconda Prompt" та запустіть його. Також це знаходиться в головному меню Anaconda.

## 2. Команди для терміналу

Скопіюйте та виконуйте ці команди по черзі в Anaconda Prompt:

```bash
# Створюємо ізольоване середовище з Python 3.11 (найбільш стабільна версія для ML)
conda create -n llm_project python=3.11 -y

# Активуємо середовище (назва в дужках має змінитись з base на llm_project)
conda activate llm_project

# Встановлюємо ядро для зв'язку середовища зі Spyder
pip install spyder-kernels==3.1.*

# Встановлюємо бібліотеки для аналізу та візуалізації
pip install transformer_lens seaborn matplotlib pandas scikit-learn

# Встановлюємо PyTorch (версія з підтримкою CUDA 12.4 для NVIDIA GPU)
pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124

pip install matplotlib seaborn plotly tqdm colorama