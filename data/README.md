# 📂 Каталог даних (Data Storage)

Тут зберігаються всі вхідні та проміжні обчислювальні дані.

## Структура

- `raw_prompts/`: Місце для JSON/CSV файлів, якщо ми вирішимо винести промти з коду.
- `activations/`: **Важливо!** Тут зберігаються збережені тензори активацій у форматі `.pt` (PyTorch) або `.npy` (NumPy).

⚠️ **Увага:** Файли в папці `activations/` можуть бути дуже великими. Вони ігноруються Git, щоб не перевантажувати репозиторій. Кожен учасник генерує їх локально за допомогою `model_inspector.py`.
